name: Linter test

on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:

      - name: Install dependencies
        run: |
          pip install poetry
      - name: Lint with flake8
        run: |
          make lint
